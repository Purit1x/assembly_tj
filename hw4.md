# HW4

## Part1

​	将整数转换为字符串进行打印的过程复用之前求和的代码。在调用转换子过程时需要传递参数，但由于call指令会进行压栈，感觉使用寄存器传递参数会更方便一点。

​	另外，在每次使用buffer进行字符串打印时，打印完成后需要将buffer清空，否则上一行最后一次的十位会影响下一行的输出。



## Part2

​	table地址使用si保存，在使用mov指令将[si]的值移动到寄存器用于与正确结果比较时，应注意移动到8位寄存器，否则移动的将是字数据（对16位系统而言）而非字节数据，无法得到正确结果。